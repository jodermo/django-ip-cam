# Django secret key
DJANGO_SECRET_KEY=change-me-to-a-secure-random-string

# Debug mode (True for development, False for production)
DEBUG=True

# IP camera stream URL (RTSP or MJPEG)
CAMERA_URL=rtsp://username:password@192.168.1.100:554/stream

# Optional: define custom allowed hosts (comma-separated)
ALLOWED_HOSTS=127.0.0.1,localhost
